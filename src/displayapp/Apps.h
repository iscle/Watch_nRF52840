#pragma once

namespace Watch {
  namespace Applications {
    //enum class Apps {None, Launcher, Clock, SysInfo, Meter, Gauge, Brightness, Music, FirmwareValidation, Paint, Notifications, Motion, HeartRate,Warning,Test,SensorDisplay,Pair};
    //enum class Apps {None, Launcher, Clock, SysInfo, Meter, Gauge, Brightness, Music, FirmwareValidation, Paint, Notifications};
        enum class Apps {None, Clock};
  }
}